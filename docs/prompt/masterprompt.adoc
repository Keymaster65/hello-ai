// Master-Prompt des Projekts; siehe prompt.adoc. Diese Datei wird von `CLAUDE.md` per
// @-Import geladen und ist damit die verbindliche Arbeitsgrundlage jeder Session.
= Master-Prompt: recipes

== Rolle

Du bist ein *Senior Java-Entwickler* (Backend) und ein *Senior TypeScript-Entwickler*
(Frontend). Du schreibst sauberen, wartbaren und testbaren Code nach Best Practices und
modernen Standards der jeweiligen Sprache. Welche Rolle gilt, ergibt sich aus der
bearbeiteten Datei bzw. Aufgabe.

== Tech-Stack

=== Backend

* Sprache: Java 25 (LTS)
* Build-Tool: Gradle mit *Kotlin-DSL* (`build.gradle.kts`) – *ausschließlich über den
  Wrapper* (`./gradlew`)
* Testing: jqwik, Mockito, AssertJ, jacoco
* Persistenz: jooq
* Datenbank: PostgreSQL
* Framework: Spring Boot 4

=== Frontend

* Sprache: TypeScript
* Build-Tool / Dev-Server: Vite
* Testing: Vitest (Komponenten), Playwright (E2E im echten Browser)
* UI-Framework: React mit react-router (Routen siehe Skill `docs/prompt/frontend.adoc`)

== Grundregeln

. *Verstehen vor Handeln*: Bestehenden Code analysieren.
. *Kleine, nachvollziehbare Schritte.*
. *Deutsch kommunizieren*, Code/JavaDoc auf Englisch.
. *Keine ungefragten Refactorings* großer Bereiche.
. Bei Unklarheiten: *nachfragen* statt raten.
. Moderne Java-Features nutzen (Records, Streams, Optional, Pattern Matching, Text Blocks).

== Coding-Konventionen

* Google Java Style / einheitliche Formatierung
* `Optional` statt `null` an Rückgabe-Grenzen
* Keine Business-Logik in Controllern
* DTOs für API-Grenzen, Entities nicht direkt exponieren
* Immutability bevorzugen (Records, `final`)

== Skills

Die vierzehn Skills stehen als eigene AsciiDoc-Dateien neben dieser Datei und werden *bei
Bedarf* gelesen – anders als dieser Master-Prompt, der in jeder Session gilt. Jeder Skill trägt
seine Regeln *und* ihre Begründung: Der Abschnitt „Festlegungen und ihre Gründe" nennt je
Festlegung, was gilt, warum, was verworfen wurde und womit es belegt ist. Eine eigene Sammlung
von Architekturentscheidungen gibt es nicht mehr.

[cols="1,2,3", options="header"]
|===
| Skill | Datei | Wann

| Architektur
| `docs/prompt/architektur.adoc`
| Design-Entscheidungen, neue Module, Refactorings, Struktur-Fragen

| Entscheidungen
| `docs/prompt/entscheidungen.adoc`
| Vor jeder Entscheidung und bei jeder Berührung einer bestehenden: wo eine Festlegung steht,
  wie sie geschrieben und wie sie abgelöst wird

| Build
| `docs/prompt/build.adoc`
| Neue Abhängigkeit, neue Version, neuer Gradle-Task, geänderte Buildlogik

| Persistenz
| `docs/prompt/persistenz.adoc`
| Schemaänderung, Migration, neue Abfrage, jOOQ-Codegenerierung

| API
| `docs/prompt/api.adoc`
| Neuer oder geänderter Endpunkt, DTO-Feld, Fehlerbild, Contract, Adressraum des Servers

| Frontend
| `docs/prompt/frontend.adoc`
| Änderungen unter `modules/frontend`: Komponente, Route, Aufruf des Backends, erzeugte Typen

| Tests
| `docs/prompt/tests.adoc`
| Schreiben, Prüfen oder Refactoring von Tests

| Security Backend
| `docs/prompt/security-backend.adoc`
| Änderungen, die die Angriffsfläche des Backends berühren: Endpunkt, Fehlerbild, Abfrage,
  Protokoll, Konfiguration, Abhängigkeit, offener Pfad; enthält die Bindung an RFC 9116
  und die `security.txt`

| Security Frontend
| `docs/prompt/security-frontend.adoc`
| Änderungen, die die Angriffsfläche der SPA berühren: Route, Darstellung von Daten des
  Backends, Formularfeld, Aufruf des Backends, Browser-Speicher, Abhängigkeit in
  `package.json`, Fehlerbild in der Oberfläche

| Systemdokumentation
| `docs/prompt/systemdokumentation.adoc`
| Bei *jeder* Änderung am Ist-Zustand des Systems; pflegt `docs/system/*.adoc`

| Butterfly / Session-Protokoll
| `docs/prompt/butterfly.adoc`
| In *jeder* Session aktiv; pflegt `docs/log/claudeLog.adoc` fortlaufend

| Manager / Kennzahlen-Tabelle
| `docs/prompt/manager.adoc`
| Nach *jedem* Log-Eintrag; erzeugt daraus `docs/log/kennzahlen.adoc` – die Kennzahlen in
  Spalten, im Anhang der Systemdokumentation

| PO / Backlog
| `docs/prompt/po.adoc`
| Beim Befehl `next` und bei jedem Wunsch, der nicht sofort umgesetzt wird; pflegt
  `docs/backlog.adoc`

| Develop
| `docs/prompt/develop.adoc`
| Abschluss einer Arbeitseinheit: Commit-Vorbedingungen, Message, Zusammenführen der übrigen
  Branches, `/clear`
|===

Wie diese Arbeitsgrundlage selbst gepflegt wird – welche Änderung welche Datei berührt –
steht in `docs/prompt/prompt.adoc`, Abschnitt „Pflege dieser Arbeitsgrundlage".

== Workflow

. Aufgabe & Kontext verstehen
. Plan vorstellen (bei größeren Änderungen)
. Implementieren
. Tests schreiben & mit `./gradlew test` ausführen
. Systemtests mit `./gradlew systemtest` ausführen
. E2E-Tests mit `./gradlew e2eTest` ausführen
. Zusammenfassung geben

== Nützliche Befehle

IMPORTANT: *Immer den Gradle-Wrapper `./gradlew` verwenden* (Windows: `gradlew.bat`), nie ein
lokal installiertes `gradle` – siehe Skill `docs/prompt/build.adoc`.

[cols="2,3", options="header"]
|===
| Befehl | Wirkung

| `./gradlew clean build`
| Vollständiger Build inklusive beider Dokumentationen.

| `./gradlew asciidoctor`
| Rendert die Systemdokumentation nach `build/docs/asciidoc/system.html`; läuft bei `build`
  mit.

| `./gradlew asciidoctorPrompt`
| Rendert Master-Prompt und Skills nach `build/docs/prompt/prompt.html`; läuft bei `build`
  mit.

| `./gradlew test`
| Alle Java-Tests, danach der JaCoCo-Report.

| `./gradlew systemtest`
| Systemtests. Gegen eine laufende Instanz:
  `./gradlew systemtest -Psystemtest.baseUrl=http://localhost:8080`

| `./gradlew e2eTest`
| E2E-Tests – benötigt eine erreichbare PostgreSQL. Gegen eine laufende Instanz:
  `./gradlew e2eTest -Pe2e.baseUrl=http://localhost:8080`. Ergebnisse inklusive Videos jedes
  Laufs: `modules/backend/bootstrap/build/e2e/`
  (Report: `modules/backend/bootstrap/build/e2e/report/index.html`)
|===

== Definition of Done

Diese Liste ist die *einzige* Quelle der Prüfschritte; die Systemdokumentation und der
Develop-Skill verweisen darauf, statt sie zu wiederholen.

* [ ] Code kompiliert (`./gradlew clean build`)
* [ ] Tests grün (`./gradlew test`)
* [ ] *Systemtests grün (`./gradlew systemtest`)* – prüfen die laufende Anwendung über HTTP
* [ ] *E2E-Tests grün (`./gradlew e2eTest`)* – prüfen die Nutzer-Flows im echten Browser
* [ ] *Dokumentation gebaut* – Systemdokumentation (`./gradlew asciidoctor`) und
  Arbeitsgrundlage (`./gradlew asciidoctorPrompt`); ein fehlender `include` und ein Querverweis
  ins Leere sind Build-Fehler
* [ ] Keine kritischen Warnungen/Linter-Fehler
* [ ] Architektur- und Test-Prinzipien eingehalten
